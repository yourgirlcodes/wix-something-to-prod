const {createStoreBuilder, createEmptyStore, isValidStore, AspectStore} = require('./lib/wix-aspect-store'),
  Aspect = require('./lib/wix-aspect'),
  createUtils = require('./lib/utils'),
  {DEFAULT_WHITELIST, DEFAULT_QUERY_PARAMS_WHITELIST} = require('./lib/raw-aspect'),
  createAspectsContext = require('./lib/wix-aspects-context');

const AspectsContext = createAspectsContext();
const utils = createUtils({AspectsContext});

module.exports = {
  createStoreBuilder,
  createEmptyStore,
  isValidStore,
  AspectsContext,
  AspectStore,
  Aspect,
  utils,
  DEFAULT_WHITELIST,
  DEFAULT_QUERY_PARAMS_WHITELIST
};
