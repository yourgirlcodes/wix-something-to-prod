function isRpc({originalUrl, headers}) {
  const [service, prefix] = (originalUrl || '/').split('/').reverse();
  return prefix === '_rpc' && !!service &&
    typeof headers === 'object' && (headers['content-type'] || '').toLowerCase().startsWith('application/json-rpc');
}

module.exports = isRpc;
