import {LintRule} from '../../../../..';
import fs from 'fs';
import path from 'path';

export const rule: LintRule = {
  description: 'ensure there is a doc.yaml file in module folder',
  async run(ctx) {
    ctx.assert(fs.existsSync(path.join(ctx.moduleDir, 'doc.yaml')));
  }
};
