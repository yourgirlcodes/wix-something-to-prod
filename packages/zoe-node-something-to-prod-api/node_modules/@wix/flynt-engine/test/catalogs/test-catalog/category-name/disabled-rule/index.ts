import {LintRule, Severity} from '../../../../..';

export const rule: LintRule = {
  description: 'this rule always fails',
  moreInfoUrl: 'http://www.alwaysfail.com',
  severity: Severity.Off,
  async run(ctx) {
    ctx.report({
      message: 'this rule always reports an issue'
    });
  }
};
