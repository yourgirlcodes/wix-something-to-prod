const rule = {
  description: 'field names that start with underscore are not allowed',
  async run(ctx) {
    ctx.visit({
      visitField(field) {
        if (field.name.startsWith('_')) {
          ctx.report({errorAt: field});
        }
      }
    });
  }
};

module.exports = {rule};

