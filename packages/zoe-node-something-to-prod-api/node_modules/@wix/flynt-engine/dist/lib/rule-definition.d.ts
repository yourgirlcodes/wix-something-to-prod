import { LintRule, RuleDefinition } from '../index';
export declare class RuleDefinitionImpl implements RuleDefinition {
    private _rule;
    readonly name: string;
    readonly path: string;
    constructor(ruleDir: string, ruleModule?: any);
    private resolveRuleModule;
    private loadRule;
    validateRule(rule: LintRule): void;
    getRule(): Promise<LintRule>;
    getDocPath(): Promise<string>;
}
