import { Visitor } from '@wix/proto-syntax-tree';
import { AddedVisitor, ModifiedVisitor, RemovedVisitor, ShouldVisit, StatefulVisitor } from '../stateful/rule-api';
import { NewStateProvider, PrevStateProvider } from './state-provider';
declare type NotUndef<F> = F extends undefined ? never : F;
export declare type ShouldVisitMethods = {
    [K in keyof ShouldVisit]-?: NotUndef<ShouldVisit[K]> extends (elem: infer E) => boolean ? K : never;
}[keyof ShouldVisit];
export declare function adaptStatefulVisitor(visitor: StatefulVisitor, ctx: PrevStateProvider): Visitor;
export declare function adaptModifiedVisitor(visitor: ModifiedVisitor, ctx: PrevStateProvider): Visitor;
export declare function adaptAddedVisitor(visitor: AddedVisitor, ctx: PrevStateProvider): Visitor;
export declare function adaptRemovedVisitor(visitor: RemovedVisitor, ctx: NewStateProvider): Visitor;
export {};
