import { LintConfig, Linter, LintResults, ProtoModule } from '../index';
export declare class ProtoLinter implements Linter {
    private readonly _config;
    constructor(config: LintConfig);
    lint(proto: ProtoModule, prevProto?: ProtoModule): Promise<LintResults>;
    private loadRule;
    private executeRule;
    private runRule;
    private updateNewResultsToError;
    private validateProtoModule;
    private getSeverity;
    private getCatalog;
}
