import { BaseCatalogImpl } from './catalog';
export declare class FsCatalog extends BaseCatalogImpl {
    private readonly ruleNames;
    private readonly catalogDocUrl?;
    constructor(catalogDir: string);
    loadRuleDefinitions(): void;
    private getRuleDirs;
    addRuleDef(ruleDir: string): void;
    getDocUrl(ruleDocPath: string): string;
    private getCatalogDocUrl;
}
