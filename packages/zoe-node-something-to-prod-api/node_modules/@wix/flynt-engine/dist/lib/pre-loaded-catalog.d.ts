import { BaseCatalogImpl } from './catalog';
interface CatalogInfo {
    rules: RuleLoaderInfo[];
}
interface RuleLoaderInfo {
    path: string;
    docUrl: string;
    ruleModule: Promise<any>;
}
export declare class PreLoadedCatalog extends BaseCatalogImpl {
    constructor(catalogDir: string, info: CatalogInfo);
    private loadRuleDefinitions;
    getDocUrl(ruleDocPath: string): string;
}
export {};
