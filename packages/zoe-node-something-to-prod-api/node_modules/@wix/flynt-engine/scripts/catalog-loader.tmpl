/* eslint-disable node/no-missing-require */
const allRules = [
{{#each rules}}
  {
    path: '{{{path}}}',
    docUrl: '{{{docUrl}}}',
    ruleModule: require('./{{{path}}}') as any,
  },
{{/each}}
];

export function getCatalogInfo() {
  return {
    rules: allRules
  };
}
