const bundled = require('./bundled'),
  {Module} = require('./module'),
  protoAstTraverser = require('./proto-ast-traverse');

module.exports = {

  listBundledProtoFiles() {
    return bundled.FILES.slice();
  },

  listBundledRoots() {
    return bundled.DIRS.slice();
  },

  protoAstTraverser,

  Module
};

