const prcs = require('process');

const ALIASES = {
  darwin_arm64: 'darwin_x64'
}

function getBinaryType({platform, arch} = prcs) {
  const originalType = `${platform}_${arch}`;
  return ALIASES[originalType] ? ALIASES[originalType] : originalType;
}

module.exports = {getBinaryType};
