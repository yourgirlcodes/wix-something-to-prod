import { FlyntCtx, FlyntPlugin } from '@wix/flynt-plugin-types';
export declare type PluginHints = {
    skipStateful: true;
} | {
    skipStateful?: false;
    workTree: string;
};
export declare class NodePlugin implements FlyntPlugin {
    readonly name = "node";
    private readonly cleanWorkTree;
    private readonly skipStateful;
    private workTree?;
    private prevMainProtoFolder?;
    constructor(hints?: PluginHints);
    init(ctx: FlyntCtx): Promise<void>;
    beforeUnload(ctx: FlyntCtx): Promise<void>;
    beforeValidateFlyntOptions(ctx: FlyntCtx): void;
    private createWorkTreeIfNeeded;
}
