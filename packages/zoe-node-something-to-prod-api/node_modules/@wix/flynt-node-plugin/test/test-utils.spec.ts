import tmp from 'tmp';
import {prepareRepo} from './test-utils';

describe('test utils', () => {
  it('create repo tester', async () => {
    //tested "manually"
    const {name: repoFolder} = tmp.dirSync({prefix: 'git-repo'});
    await prepareRepo('./test/repos/simple', repoFolder, {skipBranches: true});
  });
});
