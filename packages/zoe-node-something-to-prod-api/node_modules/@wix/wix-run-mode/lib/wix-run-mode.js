module.exports.isProduction = (effectiveEnv = process.env) => {
  return (effectiveEnv.NODE_ENV || '').toLowerCase() === 'production';
};
module.exports.isCI = (effectiveEnv = process.env) => {
  return !!(effectiveEnv.BUILD_NUMBER);
};
