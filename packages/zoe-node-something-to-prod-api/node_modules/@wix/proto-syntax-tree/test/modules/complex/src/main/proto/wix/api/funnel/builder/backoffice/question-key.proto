syntax = "proto3";

package wix.api.funnel.builder.backoffice;

import "wix/api/validations.proto";

message QuestionKey {
  string key = 1 [(wix.api.minLength) = 1, (wix.api.maxLength) = 100];

  repeated OptionKey option_keys = 2;
}

message OptionKey {
  string key = 1 [(wix.api.minLength) = 1, (wix.api.maxLength) = 100];
}
