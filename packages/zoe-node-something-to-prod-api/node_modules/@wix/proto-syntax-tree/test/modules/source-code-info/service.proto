//leading detached from svc.proto syntax

//leading from svc.proto syntax
syntax = "proto3"; //trailing from svc.proto syntax

//leading detached from svc.proto package

//leading from svc.proto package
package com.wix; //trailing from svc.proto package

import "options.proto";

//leading detached file-option

/* leading file-option */
option (options.file) = 3; // trailing file-option

service Service {

  //leading detached service-option

  /* leading service-option */
  option (options.service) = 1; // trailing service-option

  //leading detached service-option-2

  /* leading service-option-2 */
  option (options.service) = 2; // trailing service-option-2

  rpc Method (Message) returns (Response) {

    //leading detached method-option

    /* leading method-option */
    option (options.method) = {
      name: "xyz" // trailing method.name-option
      desc: "abc" // trailing method.desc-option
    }; // trailing method-option
  }

  rpc Method2 (Message) returns (Response) {

    //leading detached method.name-option

    /* leading method.name-option */
    option (options.method).name = "xyz"; // trailing method.name-option

    //leading detached method.desc-option

    /* leading method.desc-option */
    option (options.method).desc = "abc"; // trailing method.desc-option
  }
}

message Message {

  //leading detached message-option

  /* leading message-option */
  option (options.message) = {name: 'name1' }; // trailing message-option

  //leading detached message-option2

  /* leading message-option2 */
  option (options.message) = {name: 'name2' }; // trailing message-option2

  int32 num = 1 [(options.field) = 'f1' /* trailing field-option */];
  oneof one_of {

    //leading detached oneof-option

    /* leading oneof-option */
    option (options.one_of) = 'one_of'; // trailing oneof-option
    string str = 2;
  }
}

message Response {
}

enum SomeEnum {

  //leading detached enum-option

  /* leading enum-option */
  option (options.enum).value = VALUE1; // trailing enum-option

  VALUE = 0 [(options.enum_value) = VALUE1 /* trailing enum-value-option */];
}
