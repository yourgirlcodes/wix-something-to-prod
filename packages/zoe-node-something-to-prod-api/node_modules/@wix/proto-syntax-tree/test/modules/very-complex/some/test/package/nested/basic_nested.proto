syntax = "proto3";

package some.test.package.nested;

message Root {
    Nested one = 1;
    Nested.Nested two = 2;
    Nested.Nested.Nested three = 3;
    SomewhereElse else = 99;

    message Nested {
        message Nested {
            message Nested {
            }
        }
    }
}

message SomewhereElse {
    Root.Nested one = 1;
    Root.Nested.Nested two = 2;
    Root.Nested.Nested.Nested three = 3;
}
