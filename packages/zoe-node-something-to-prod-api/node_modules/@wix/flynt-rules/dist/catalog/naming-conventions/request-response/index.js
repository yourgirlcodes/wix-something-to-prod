"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rule = void 0;
const fqdn_utils_1 = require("../../fqdn-utils");
const rule = {
    description: 'Request/Response message names should follow method name',
    moreInfoUrl: 'https://bo.wix.com/wix-docs/rnd/platformization-guidelines/protobuf#platformization-guidelines_protobuf_methods',
    run(ctx) {
        ctx.visit({
            visitMethod(method) {
                if ((0, fqdn_utils_1.isSubscription)(method)) {
                    return false;
                }
                ctx.assert(method.request.name === method.name + 'Request', { errorAt: method, message: `expected request to be named '${method.name}Request'` });
                ctx.assert(method.response.name === method.name + 'Response', { errorAt: method, message: `expected response to be named '${method.name}Response'` });
            }
        });
    }
};
exports.rule = rule;
//# sourceMappingURL=index.js.map