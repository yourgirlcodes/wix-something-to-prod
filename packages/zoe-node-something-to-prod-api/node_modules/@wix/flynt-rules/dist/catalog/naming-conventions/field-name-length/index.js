"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rule = void 0;
const flynt_engine_1 = require("@wix/flynt-engine");
const rule = {
    description: 'Field name length must not exceed 64 characters',
    severity: flynt_engine_1.Severity.Error,
    run(ctx) {
        ctx.visit({
            visitField(field) {
                ctx.assert(field.name.length <= 64, {
                    errorAt: field,
                    message: `field name length must not exceed 64 characters. field '${field.name}' is ${field.name.length} characters long`
                });
            }
        });
    }
};
exports.rule = rule;
//# sourceMappingURL=index.js.map