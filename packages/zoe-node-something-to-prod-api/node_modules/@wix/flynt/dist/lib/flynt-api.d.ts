import { FlyntPlugin } from '@wix/flynt-plugin-types';
import { CliOptions } from './flynt-cli';
import { LintResults, Severity } from '@wix/flynt-engine';
export interface FlyntApiOpts extends CliOptions {
    plugins?: FlyntPlugin[];
}
export interface FlyntResults {
    output: FlyntOutput;
    formattedOutput: string;
    warnings: number;
    errors: number;
    strictErrors: number;
}
export interface FlyntOutput {
    lintResults: LintResults;
    config: LintConfig;
    [key: string]: any;
}
export interface LintConfig {
    rules: {
        [ruleName: string]: Severity | undefined;
    };
    date?: Date;
    severity?: Severity;
}
export declare function flynt(opts: FlyntApiOpts): Promise<FlyntResults>;
