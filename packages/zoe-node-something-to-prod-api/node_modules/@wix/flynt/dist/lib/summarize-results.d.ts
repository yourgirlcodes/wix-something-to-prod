import { LintResult, LintResults, Severity } from '@wix/flynt-engine';
import { FlyntPlugins } from '@wix/flynt-plugins-host';
export interface LintResultsSummary {
    formattedOutput: string;
    warnings: number;
    errors: number;
    strictErrors: number;
    inGracePeriod: number;
}
export declare function groupBySeverityAndRuleId(lintResults: LintResults): Map<Severity, Record<string, LintResult>>;
export interface SummaryOptions {
    moduleRootFolder: string;
}
export declare function summarizeResults(lintResults: LintResults, opts: SummaryOptions, plugins: FlyntPlugins): LintResultsSummary;
