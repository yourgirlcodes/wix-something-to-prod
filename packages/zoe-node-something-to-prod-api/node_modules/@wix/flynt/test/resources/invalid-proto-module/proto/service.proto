syntax = "proto3";

package foo.bar;

import "wix/api/validations.proto";
import "google/protobuf/wrappers.proto";


service SomeService {
  rpc Method (Message) returns (Message) {
    option (.wix.api.option_not_exists) = 1;
  }
}

message Message {
  google.protobuf.StringValue str = 1;
}
