syntax = "proto3";

package com.wix.bookings.schedules.api.v1;

import "google/protobuf/wrappers.proto";

message ParticipantNotification {
    bool notify_participants = 1; // Whether to notify participants about the change.
    google.protobuf.StringValue message = 2; // A custom message to send to the participants.
}
