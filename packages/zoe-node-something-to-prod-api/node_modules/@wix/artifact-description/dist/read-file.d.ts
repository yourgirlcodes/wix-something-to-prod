export interface FileEntry {
    content: string;
    location: string;
    dir: string;
    subPath: string;
    isPresent: boolean;
}
export declare function readFile(dir: string, subPath: string): FileEntry;
