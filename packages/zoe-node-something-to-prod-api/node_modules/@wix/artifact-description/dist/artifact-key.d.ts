/**
 * The ID of services in Wix's production system
 */
export interface ArtifactKey {
    /** A globally unique non-empty string. Its length exceed 200 characters. */
    groupId: string;
    /** A non empty string that is unique within the group. Its length exceed 200 characters. */
    artifactId: string;
}
export declare const GROUP_ID_REGEX: RegExp;
export declare const ARTIFACT_ID_REGEX: RegExp;
