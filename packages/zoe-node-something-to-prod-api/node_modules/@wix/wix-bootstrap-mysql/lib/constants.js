const MIN_TTL_IN_MILLIS = 5000;             // 5 seconds
const MAX_TTL_IN_MILLIS = 30 * 60 * 1000;   // 30 minutes
const WIX_METERING_START_SYMBOL = Symbol('wix_metering_start');

module.exports = {MIN_TTL_IN_MILLIS, MAX_TTL_IN_MILLIS, WIX_METERING_START_SYMBOL};
